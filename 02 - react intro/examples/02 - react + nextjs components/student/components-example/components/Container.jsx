export default function Container( {children} ) {
    // 'childre' is a hardcoded prop for anything nested inside that component when its called.
    // it is autmatically 
    return(
        <>
        {children}
        </>
    )
}